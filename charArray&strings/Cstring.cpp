#include<iostream>
#include<cstring>
using namespace std;
int main()
{
    char str1[30];
    strcpy(str1,"deven");
    cout<<str1;

    //strcpy(dest,src) - string copy
    //strcat(str1,str2)- concatinate two strings and strore it in first string.
    /*strcmp(str1,str2)-compare two strings based on values (-ve,0,+ve)
    if 0 = equal
    -ve = if first str is smaller
    
    */ 

     
    
 return 0;
}