#include<iostream>
using namespace std;
#include<string>
int main()
{
    cout<<"the one's completement of 6 is: "<<(~6)<<endl; 
    cout<<"the one's completement of 0 is: "<<(~0)<<endl; 
    
    
 return 0;
}